<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ParagonWiki.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ParagonWiki"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage"
        Icon="search_icon.png"/>

    <ShellContent
        Title="Equipment"
        ContentTemplate="{DataTemplate local:Equipment}"
        Route="Equipment"
        Icon="equipment_icon.png"/>-->

    <TabBar>
        <ShellContent Title="Home Page" Icon="dotnet_bot"
                         ContentTemplate="{DataTemplate local:MainPage}" />

        <ShellContent Title="Tryout" Icon="dotnet_bot" ContentTemplate="{DataTemplate local:Equipment}" />
        <ShellContent Title="About" Icon="dotnet_bot" ContentTemplate="{DataTemplate local:AboutPage}"/>
    </TabBar>
    
</Shell>
